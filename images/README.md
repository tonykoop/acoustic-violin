# Images

Placeholder. No real shop or finished-instrument photos exist yet for this
build. Concept renders and hero photos are intentionally absent so the
readiness banner in `README.md` is not undermined by stock or unattributed
imagery.

`photo-shotlist.md` at the repo root holds the required photo sequence. Save
future captures here with stable, descriptive names, for example:

- `hero-acoustic-violin.jpg`
- `plate-graduation-top.jpg`
- `plate-graduation-back.jpg`
- `neck-set-projection.jpg`
- `bridge-fit-feet.jpg`
- `soundpost-position.jpg`
- `tuning-verification-open-strings.jpg`

Until prototypes exist, the repo uses textual references (design table,
drawing brief, validation CSV) instead of generated imagery.
